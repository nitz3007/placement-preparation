#include <iostream>
using namespace std;

int main() {
	int a,b,c,max=0,sc=0;
	cin>>a>>b>>c;
	if(a<b)
	{
	    if(b<c)
	        sc=b;
	    else sc= (a<c ? c:a);
	}
	else
	{
	    if(a<c)
	        sc=a;
	    else sc=(b<c ? c:b);
	}
	cout<<sc<<endl;
	return 0;
}
